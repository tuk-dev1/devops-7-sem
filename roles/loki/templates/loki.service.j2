[Unit]
Description=Loki service
After=network.target

[Service]
Type=simple
ExecStart={{ loki_install_dir }}/loki-linux-amd64 -config.file={{ loki_config_file }}
Restart=on-failure

[Install]
WantedBy=multi-user.target
